rootUrl: http://localhost:8080/components/vaadin-element/test/visual/
gridUrl: http://localhost:4444/wd/hub
screenshotsDir: ./test/visual/screens

system:
  plugins:
    polyserve:
      port: 8080
    selenium: true

browsers:
  chrome:
    desiredCapabilities:
      browserName: "chrome"
      chromeOptions:
        args:
          - headless
